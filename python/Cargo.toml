[package]
name = "migration-engine-py"
version = "0.1.1"
edition = "2021"

[lib]
crate-type = ["cdylib"]
name = "migration_engine_py"

[dependencies]
pyo3 = { version = "0.21", features = ["extension-module"] }
migration-engine = { path = ".." }

[build-dependencies]
pyo3-build-config = "0.21"
